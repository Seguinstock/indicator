name: Continuous RS 3Y Stagnation Exit Comparison

on:
  issues:
    types: [opened, reopened]

permissions:
  contents: write
  issues: write

jobs:
  backtest:
    if: github.event.issue.title == 'Run Continuous RS 3Y Stagnation Exits'
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pitindex lxml
      - name: Run stagnation-exit variants
        run: python engine/run_rs3y_stagnation.py
      - name: Publish results
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add data/backtest_relative_strength_continuous_3y_surge15_6_tech_stale*.json
          git commit -m "Publish continuous RS 3Y stagnation-exit comparison" || true
          git pull --rebase origin main
          git push
      - name: Close request
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ "${{ job.status }}" = "success" ]; then
            gh issue comment "${{ github.event.issue.number }}" --body "Continuous RS 3Y stagnation-exit comparison completed for 60/90/120/180 sessions."
          else
            gh issue comment "${{ github.event.issue.number }}" --body "Stagnation-exit comparison ended with status: ${{ job.status }}. Check the Actions log."
          fi
          gh issue close "${{ github.event.issue.number }}"
