name: Explore S&P 1500 microtest Lot B1 RSI

on:
  issues:
    types: [opened]

permissions:
  contents: write
  issues: write

jobs:
  explore:
    if: github.event.issue.title == 'Run Stock Indicator Microtest Lot B1'
    runs-on: ubuntu-latest
    timeout-minutes: 350
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pitindex
      - name: Run Lot B1 RSI exploratory study
        env:
          MICROTEST_SEED: '20260910'
          MICROTEST_LOT: 'B1'
        run: python engine/microtest_lot.py
      - name: Publish Lot B1 result
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          cp data/microtest_lot_b1.json /tmp/microtest_lot_b1.json
          git fetch origin main
          git reset --hard origin/main
          cp /tmp/microtest_lot_b1.json data/microtest_lot_b1.json
          git add data/microtest_lot_b1.json
          if git diff --cached --quiet; then
            echo "Lot B1 result already matches main"
          else
            git commit -m "Publish S&P1500 microtest Lot B1 RSI results"
            git push origin HEAD:main
          fi
      - name: Close request
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh issue comment "${{ github.event.issue.number }}" --body "S&P 1500 microtest Lot B1 RSI completed. Results: data/microtest_lot_b1.json"
          gh issue close "${{ github.event.issue.number }}"
