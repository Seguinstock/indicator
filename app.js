async function loadResults(){const r=await fetch("data/results.json",{cache:"no-store"});const d=await r.json();document.getElementById("updated").textContent="Mise à jour : "+(d.updated||"—");render("buyList",d.buy||[]);render("sellList",d.sell||[])}function render(id,rows){const el=document.getElementById(id);el.innerHTML="";rows.forEach(x=>{const row=document.createElement("div");row.className="row";const s=document.createElement("div");s.className="symbol "+(x.country==="CA"?"canada":x.country==="US"?"usa":"other");s.textContent=x.symbol;const sc=document.createElement("div");sc.className="score";sc.textContent=Number(x.score).toFixed(1);row.append(s,sc);el.appendChild(row)})}loadResults().catch(console.error);